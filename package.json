{
  "name": "@holochain-engineers/invitations-dev",
  "private": true,
  "workspaces": [
    "ui",
    "tests"
  ],
  "scripts": {
    "start": "npm run build:happ && cross-env HC_PORT=$(port) concurrently -k \"npm run start:happ\" \"npm run start -w @holochain-engineers/invitations\"",
    "test": "npm run build:happ && npm t -w @eyss/invitations-tests",
    "start:ui": "npm run start -w @holochain-engineers/invitations",
    "start:mock": "npm run mock -w @holochain-engineers/invitations",
    "start:happ": "rimraf .hc* && RUST_LOG=warn hc s generate workdir/happ/invitations.happ --run=8888 -a test-app network --bootstrap https://bootstrap-staging.holo.host/ quic",
    "build:happ": "npm run build:dna && hc app pack workdir/happ",
    "build:dna": "npm run build:zome && hc dna pack workdir/dna",
    "build:zome": "CARGO_TARGET_DIR=target cargo build --release --target wasm32-unknown-unknown"
  },
  "devDependencies": {
    "concurrently": "^6.2.1",
    "cross-env": "^7.0.3",
    "new-port-cli": "^1.0.0",
    "rimraf": "^3.0.2"
  }
}
